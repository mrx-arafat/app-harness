// slop-rust - planted smells: hardcoded /Users path, no flag parsing or usage,
// no error handling. (Never built; quality only scans source.)
fn main() {
    let path = "/Users/example/data/config.json";
    println!("{path}");
}
