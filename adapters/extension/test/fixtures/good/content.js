// Minimal content script for the harness test fixture — no DOM mutation needed,
// its mere presence registers an isolated execution world that verify.sh detects.
console.log('harness fixture content script loaded');
